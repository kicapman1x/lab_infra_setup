1. Download bind and bind-utils
2. Set up the dns server as shown (with named.conf and the db file)
3. Set up the server using the dns server with resolv.conf
4. Note that all files there the IP is internal local IP, dont just copy.
5. Note, if u using tailscale, dont allow it to manage ur dns for you - go and use: 'tailscale up --accept-dns=false'