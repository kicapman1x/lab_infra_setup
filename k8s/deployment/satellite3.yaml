apiVersion: apps/v1
kind: Deployment
metadata:
  name: satellite3
  labels:
    app: satellite3
    appCode: load-generator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: satellite3
      appCode: load-generator
  template:
    metadata:
      labels:
        app: satellite3
        appCode: load-generator
    spec:
      containers:
      - name: satellite3
        image: kicapman1x/load-generator:satellite3-rc1
        imagePullPolicy: Always
        envFrom:
        - configMapRef:
            name: loadgen-config
        - secretRef:
            name: loadgen-secret
        env:
        - name: PYTHONUNBUFFERED
          value: "1"
        volumeMounts:
        - name: load-gen-data
          mountPath: /data/load_gen
      volumes:
      - name: load-gen-data
        persistentVolumeClaim:
          claimName: load-gen-pvc